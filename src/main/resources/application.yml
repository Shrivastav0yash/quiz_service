spring:
  application:
    name: Quiz-Service

  datasource:
    url: jdbc:mysql://localhost:3306/Microservice_quizdb
    username: root
    password: Yash123@

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    defer-datasource-initialization: true

  sql:
    init:
      mode: never
      data-locations: classpath:data.sql

server:
  port: 8090